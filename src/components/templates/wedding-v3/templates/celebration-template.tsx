"use client";

import { createWeddingTemplate } from "../createWeddingTemplate";
import { CELEBRATION } from "../definitions/celebration";

export const WeddingCelebrationTemplate = createWeddingTemplate(CELEBRATION);
