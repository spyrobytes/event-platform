"use client";

import { createWeddingTemplate } from "../createWeddingTemplate";
import { FINE_ART } from "../definitions/fine-art";

export const WeddingFineArtTemplate = createWeddingTemplate(FINE_ART);
