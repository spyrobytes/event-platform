"use client";

import { createWeddingTemplate } from "../createWeddingTemplate";
import { INTIMATE_NOTE } from "../definitions/intimate-note";

export const WeddingIntimateNoteTemplate = createWeddingTemplate(INTIMATE_NOTE);
