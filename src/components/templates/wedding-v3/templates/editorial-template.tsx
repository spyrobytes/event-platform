"use client";

import { createWeddingTemplate } from "../createWeddingTemplate";
import { EDITORIAL } from "../definitions/editorial";

export const WeddingEditorialTemplate = createWeddingTemplate(EDITORIAL);
